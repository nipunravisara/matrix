import React from 'react';
import {MJumbotron, MSheet} from '../../../../';

export default function JumbotronShowcase() {
  return (
    <MSheet styles={{paddingTop: 20}}>
      <MJumbotron title={'This is jumbotron'} />
    </MSheet>
  );
}
